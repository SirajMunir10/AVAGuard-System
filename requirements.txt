# AVAGuard Web Portal — Vercel Runtime Dependencies
# ONLY packages needed by the Django web portal at runtime.
# Heavy ML/AI packages (faiss, sentence-transformers, azure-identity,
# reportlab, sqlalchemy, etc.) are excluded — they blow Vercel's 250 MB limit.

# Django
Django>=4.2,<5.0
python-dotenv>=1.0.0
whitenoise>=6.6.0

# REST
djangorestframework>=3.14.0
djangorestframework-simplejwt>=5.3.0
django-cors-headers>=4.3.0

# Database
psycopg2-binary>=2.9.9

# Auth / OTP (loaded only when OTP_ENABLED=True)
pyotp>=2.9.0
qrcode>=7.4.2
Pillow>=10.0.0

# Utils
requests>=2.31.0
python-dateutil>=2.8.2
openpyxl>=3.1.2
Jinja2>=3.1.0
packaging>=23.1

# AI (API-only, no local models)
openai>=1.0.0
