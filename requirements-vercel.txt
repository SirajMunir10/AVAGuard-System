# AVAGuard Web Portal — Vercel Production Requirements
# Only what the Django web portal needs at runtime on Vercel.
# Heavy AI/ML packages (faiss, sentence-transformers, etc.) are excluded
# because they exceed Vercel's 250 MB lambda limit.

# Django core
Django>=4.2,<5.0
python-dotenv>=1.0.0

# REST API
djangorestframework>=3.14.0
djangorestframework-simplejwt>=5.3.0
django-cors-headers>=4.3.0

# Database
psycopg2-binary>=2.9.9

# Auth / Security
pyotp>=2.9.0
qrcode>=7.4.2

# Utils
requests>=2.31.0
python-dateutil>=2.8.2
openpyxl>=3.1.2
Jinja2>=3.1.0

# AI API client only (no local models)
openai>=1.0.0
